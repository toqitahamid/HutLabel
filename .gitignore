node_modules
dist
.venv
.env.local
.env
*.local
.DS_Store
# Serena LSP tool state
.serena
# Local dev tileset decoded from demo_pyramid.json (regenerate with npm run tiles:demo)
public/tiles
# Source orthomosaics — 15 GB of GeoTIFFs, never committed
data
.vercel
# Vendored AI-agent skill docs (restore with `npx skills add clerk/skills`;
# pinned by skills-lock.json, which IS committed)
.agents
.claude/skills
